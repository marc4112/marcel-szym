#include<stdio.h>

int liczba(int a, int b, int c) {
    int d;
    for (d = a; ; e++) {
        int potega = 1;

        for (int i = 0; i < c; i++)
        potega *= e;

        if (potega > b)
        return d;
    }
}